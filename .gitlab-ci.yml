image: cypress/base:10

stages:
  - install
  - test

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - node_modules

build:
  stage: build
  script:
    - npm ci

test:
  stage: test
  script:
    - npm run
    
  artifacts:
    when: always
    paths:
      - cypress/videos/**/*.mp4
      - cypress/screenshots/**/*.png
      - cypress/results/
    expire_in: 1 day